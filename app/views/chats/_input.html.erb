<div class="chatFooter">
  <div class="col-3">
    <%# <div data-action="click->recognize-speech#recognize" class="btn btn-icon btn-primary rounded"> %>
      <i class="fas fa-microphone"></i>
    <%# </div> %>
  </div>
  <%= simple_form_for @message, remote: true, html: { class: "" } do |f| %>

    <div v class="form-group boxed">
      <div class="input-wrapper">
        <%= f.input :content, placeholder: "Send Message..", as: :string, label: false, input_html: {class: "form-control chat-input", data: {recognize_speech_target:"input"}}, autofocus: true %>
     </div>
    </div>

      <button type="submit" class="btn btn-icon btn-primary rounded glow">
       <i class="fas fa-paper-plane"></i>
      </button>

  <% end %>
</div>
